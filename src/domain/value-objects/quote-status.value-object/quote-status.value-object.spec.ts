import { QuoteStatusValueObject } from './quote-status.value-object';

describe('QuoteStatusValueObject', () => {
  it('should be defined', () => {
    expect(new QuoteStatusValueObject()).toBeDefined();
  });
});
